# AI Gateway Environment Variables - Example Template
#
# ==============================================================================
# LAYERED CONFIGURATION SYSTEM
# ==============================================================================
# This project uses a layered .env file system:
#
# 1. .env                  - Base settings (common across all environments)
# 2. .env.development      - Development-specific overrides
# 3. .env.production       - Production-specific overrides
# 4. .env.test             - Test-specific overrides (optional)
#
# Files are loaded in this order:
#   .env → .env.{ENVIRONMENT} → Environment Variables
#
# Later sources override earlier ones.
#
# ==============================================================================
# QUICK START
# ==============================================================================
# For development (default):
#   1. Base .env file already contains common settings
#   2. .env.development contains dev-specific overrides
#   3. Just run: cargo run
#
# For production:
#   1. Set ENVIRONMENT=production in your deployment (docker-compose, k8s, etc.)
#   2. .env.production will be loaded automatically
#   3. Override sensitive values with environment variables
#
# ==============================================================================

# Environment Selector
# Set this in your shell/docker to choose which .env.{environment} file to load
ENVIRONMENT=development

# Server Configuration
AI_GATEWAY_SERVER_HOST=0.0.0.0
AI_GATEWAY_SERVER_PORT=3001

# Database Configuration - MongoDB
# Connection string is built from these components: mongodb://username:password@url:port/database?authSource=auth_source
AI_GATEWAY_DATABASE_MONGODB_URL=localhost                    # MongoDB host (without mongodb:// prefix)
AI_GATEWAY_DATABASE_MONGODB_PORT=27017                       # MongoDB port
AI_GATEWAY_DATABASE_MONGODB_USERNAME=admin                   # MongoDB username
AI_GATEWAY_DATABASE_MONGODB_PASSWORD=admin123               # MongoDB password
AI_GATEWAY_DATABASE_MONGODB_DATABASE=llm_hub_dev            # Database name
AI_GATEWAY_DATABASE_MONGODB_AUTH_SOURCE=admin               # Authentication database (usually "admin")
AI_GATEWAY_DATABASE_MONGODB_CONNECTION_TIMEOUT_MS=10000     # Connection timeout in milliseconds
AI_GATEWAY_DATABASE_MONGODB_MAX_POOL_SIZE=10                # Maximum connection pool size
AI_GATEWAY_DATABASE_MONGODB_MIN_POOL_SIZE=1                 # Minimum connection pool size

# Security Configuration
# ⚠️ CRITICAL: Must use the same variable name as Backend for compatibility
# Generate a new encryption key with: openssl rand -base64 32
# NEVER commit the actual key to version control!
# This key MUST match Backend's PROJECT_API_KEY_ENCRYPTION_KEY
PROJECT_API_KEY_ENCRYPTION_KEY=REPLACE_WITH_YOUR_BASE64_ENCODED_32_BYTE_KEY

# Provider API Keys (Optional - can also be configured per-project in database)
# AI_GATEWAY_PROVIDERS_OPENAI_API_KEY=sk-...
# AI_GATEWAY_PROVIDERS_ANTHROPIC_API_KEY=sk-ant-...
# AI_GATEWAY_PROVIDERS_GOOGLE_API_KEY=...

# Logging
RUST_LOG=ai_gateway=debug,tower_http=debug